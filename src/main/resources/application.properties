server.port=8080

# For a pooling DataSource to be created we need to be able to verify that a valid Driver class is available, 
# so we check for that before doing anything. 
# I.e. if you set spring.datasource.driver-class-name=com.mysql.jdbc.Driver then that class has to be loadable.
spring.datasource.driverClassName=com.mysql.jdbc.Driver

# Basic database connection information
spring.datasource.url=jdbc:mysql://localhost:3306/SpringTestDB
spring.datasource.username=SpringTutorialClient
spring.datasource.password=i-ruv_k0reA*!&!G!

# Number of ms to wait before throwing an exception if no connection is available.
spring.datasource.tomcat.max-wait=10000

# Maximum number of active connections that can be allocated from this pool at the same time.
spring.datasource.tomcat.max-active=3

# Validate the connection before borrowing it from the pool.
spring.datasource.tomcat.test-on-borrow=true